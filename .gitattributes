# 统一换行符：仓库内一律 LF，检出时也保持 LF（跨平台一致，diff 干净）
* text=auto eol=lf

# 二进制 / 生成物不要做文本转换
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件与语言统计
pnpm-lock.yaml linguist-generated=true
